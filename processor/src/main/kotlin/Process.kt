import com.atul.JavaOpenCV.ImShow
import com.google.gson.JsonObject
import org.opencv.core.*

val gui = ImShow("Kotlin-Node bridge")

// Process image here
fun Process(imgMat:Mat): JsonObject{
    val result = JsonObject()

    return result
}